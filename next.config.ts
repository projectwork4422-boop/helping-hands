import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  // Restart triggered to reload Prisma schema with BookingStatus changes
};

export default nextConfig;
